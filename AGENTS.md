React frontend for a publication system that demonstrates publication workflows, authentication, and access control.

## Essentials
- Use `bun` only; do not use `npm`, `yarn`, or `pnpm`.
- Use Biome for formatting and linting.

## Detailed guidance
- Source of truth for detailed agent instructions: `.cursor/rules/`
- If you need implementation details, open the matching `.mdc` rule file instead of adding more root-level guidance.
